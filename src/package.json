{
  "name": "perspective-ui",
  "version": "1.0.0",
  "description": "Parquet file viewer using Perspective",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron . --debug",
    "build": "npx tailwindcss -i ./app.css -o ./dist/output.css",
    "watch": "npx tailwindcss -i ./app.css -o ./dist/output.css --watch"
  },
  "dependencies": {
    "electron": "^25.0.0"
  },
  "devDependencies": {
    "tailwindcss": "^3.3.0"
  }
}